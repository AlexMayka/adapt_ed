services:
  app:
    build: .
    container_name: adapt_ed_backend
    restart: unless-stopped
    env_file:
      - ./infra/.env
    ports:
      - "${APP_PORT:-8000}:8000"
    networks:
      - adapt_ed_net

networks:
  adapt_ed_net:
    external: true
